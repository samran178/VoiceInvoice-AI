By building this with the browser's native Web Speech API and a client-side PDF generator (jspdf), it runs entirely in the browser. It costs you $0 in server fees or Replit credits to host, and it costs $0 in API fees to run. You can charge a contractor a clean monthly subscription or a one-time fee to set it up for their business.

🚀 Paste This Complete, Zero-Credit Prompt Into Replit Agent:
Plaintext
Please build a 100% client-side, mobile-responsive application called "VoiceInvoice AI" using React and Tailwind CSS. This tool allows field technicians (plumbers, electricians) to dictate their work using their voice, automatically format it into clean line items, and generate a downloadable PDF invoice entirely inside the browser without consuming any Replit server compute credits.

Include the 'jspdf' library in the dependencies to handle client-side PDF generation natively.

The design language must be highly professional, modern, and rugged: a deep slate-900 background, crisp white cards, and vibrant emerald green accents representing active currency/billing streams.

### 1. FILE ARCHITECTURE
Organize the workspace using this pure frontend structure:
- `src/components/VoiceInput.jsx` (Handles native browser Web Speech API transcription)
- `src/components/InvoicePreview.jsx` (Renders the interactive receipt structure and handles PDF generation)
- `src/App.jsx` (Orchestrates shared state like client details, job descriptions, and pricing totals)

### 2. COMPONENT SPECIFICATIONS

#### A. Voice Input Engine (`src/components/VoiceInput.jsx`)
- Create a user-friendly job details form that stacks vertically into a single column on mobile screens.
- Implement a prominent, glowing microphone button. Use the browser's native 'window.webkitSpeechRecognition' or 'window.SpeechRecognition' API to listen to the user's voice in real-time.
- When recording, make the microphone pulse red and stream the live transcript directly into a visible "Job Summary" textarea block.
- Add a text field for "Client Name", "Job Rate per Hour", and "Hours Worked".

#### B. The Live Invoice Inspector (`src/components/InvoicePreview.jsx`)
Wrap this panel inside a crisp white card styled to look exactly like a real corporate invoice receipt.
- Display the business parameters dynamically based on the inputs: Client Name, Date, Hourly Subtotals, and a bolded "Grand Total" calculated via state hooks.
- Create an action button labeled "Download PDF Invoice". Use the 'jspdf' library to construct an elegant, well-aligned PDF file containing the business layout and automatically download it to the device when clicked.

### 3. GLOBAL RESPONSIVE CONTROLS
Ensure the entire interface is optimized for field workers using smartphones on active construction sites. Elements must feature extra large tap targets (minimum 48px height), font sizes must scale fluidly, and paddings must switch from p-4 on mobile to p-8 on desktop display frameworks. Wire all state bindings seamlessly into `src/App.jsx`.
💡 How to Turn This Into Cold, Hard Cash:
Deploy It Globally for Free: Once Replit Agent finishes building the app, click Deploy and choose the Static Website option. Replit hosts static frontend sites completely free, 24/7.

Find Your First Client: Think of local service businesses in your area (AC repair shops, electricians, car mechanics, or independent plumbers).

The Pitch: Call or visit them and say:

"Hey, I noticed your field techs spend a lot of time typing up manual job summaries for invoices when they finish a project. I built a mobile tool where they can literally just talk into their phone for 10 seconds, and it instantly types out the work logs, calculates the hourly rate, and generates a professional PDF invoice right on the spot. I can customize it with your company logo and colors for a flat $25 a month."

Get just 4 local contractors to use this app, and you've unlocked $100/month in pure passive income as a student, with absolutely zero server costs running in the background.